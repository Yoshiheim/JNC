# JNC - JavaScript Not C!
## this is shitly compiler written in javascript which compile to NASM for linux x86_64
### you must have: Node.js(run compiler), Nasm(target language), and GCC(for external functions like printf in NASM)
